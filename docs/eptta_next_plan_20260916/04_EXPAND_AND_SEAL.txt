执行00_PLAN.md的工单4。
mm批准完整实验准备计划SHA-256：<实际完整hash>。
按其中授权执行剩余源训练、基线验证和新目标snapshot审批/发布，不扩大资源范围。

1. 保留AASIST/SSL-AASIST及原主次定位，补齐每底座3个独立源seed。
   已批准seed列表优先；没有时登记现有seed，再按17/29/43固定顺序取未用值补齐并先锁定。
   同一父子run谱系不是两个seed；recipe实质变化单独说明，不能只当随机波动。
2. 每底座只在预定先导seed上做源方法选择。配置固定到该底座其他seed和所有目标。
   各checkpoint分别做R4、源统计、源阈值；所有比较方法共享同底座同seed同checkpoint。
3. 从原registry读五项published清单，核验作者代码/目标函数/更新范围/视图/归一化/reset。
   内部变体和原算法、原生online/batch和episodic分开标注。
   更新encoder的方法跑真实encoder，不用冻结缓存冒充。缺失项留NOT_RUN/BLOCKED，不删除。
4. 保留源snapshot，通过现有增量接口绑定四个主目标：2019官方eval全量，
   2021DF/LA的官方subset=eval，固定版本In-the-Wild完整评价清单。
   范围、计数、标签/预处理、metadata hash、speaker/group及血缘在评分前固定。
5. 2019保留已暴露参照说明，2021相关来源如实记录，In-the-Wild未参与开发须核对访问史。
   WaveFake与CodecFake扩展在本轮封存前明确纳入/不运行/阻塞及理由，不根据结果后选。
6. 补足必要adapter、无标签权限、方法移植、计划seal和统计/成本入口的最小代码及测试。
   真CLI经--help/preview验证；配置全部具体化，不留UNBOUND偷偷运行默认值。
7. R7封存所有目标snapshot、方法/参数/seed/checkpoint/源资源、score方向/阈值、
   视图及随机性、预处理和长度策略、reset/回退、指标/分组重采样、成本、权限、预算与代码。
   独立保存暴露/血缘记录；逻辑用途覆盖不能改写旧snapshot role和历史。

交付真实完整矩阵、方法parity、目标范围表、R7计划与完整SHA-256。
缺核心目标或关键基线要明确承认计划未完整，不能靠删行假装就绪。
停止等待确认性评分授权，不先查看任何新目标效果。
