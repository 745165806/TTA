执行00_PLAN.md的工单3。
mm批准源先导计划SHA-256：<填写本项目真实计划完整hash>。
只使用计划中真实R4合格的checkpoint与其实际seed，不用未FINALIZED模型代替。

1. 按DESIGN/角色合同从允许的源数据计算每checkpoint自己的U/M/Fisher/static R等，
   cal0按预定规则确定源tau0。保持audit原用途，不读取新目标效果，不拟合目标统计。
2. 先小样本真实缓存parity与容量检查，再构建获准源缓存。
   实测K=0、逐样本reset、A→B→A、乱序、batch/分片、坏样本隔离、score方向与冻结不变性。
3. 有现成获批环境/网格时沿用。否则先按计划固定clean/noise20/band8、随机性和预处理版本。
   当前输入就是最终原视图；加噪输入不能偷偷用clean版本评分。
4. 运行Frozen、同视图平均但无更新、完整EP、static R、原scalar/subspace/P0/P1、
   单独去关键保护项和已有相同变量简单目标对照。复用原定义，不臆造数学或published身份。
5. EP默认9个候选：K={1,3,5}；eta={0.3,1,3}乘已明确基础学习率。
   其他参数固定，原17点scalar等既有对照保留。不得自行扩大搜索。
6. 每轮select评分先封存，再由独立select evaluator读标签。
   按已批准的三条件平均EER、数值与clean退化门槛选择，保留所有候选。
   无可行配置时提交有限源域诊断，不弱化底座，不用目标救选参数。
7. 交付全候选EER/AUROC、固定源阈值两类错误率、按类有益/有害翻转、
   适配成功/安全不更新/数值回退、约束诊断、额外视图及端到端成本。
8. 缺条件转换、view-average、runner/selector、统计或真实入口则补最小代码与针对性测试。
   合成、真实源实验、未执行内容分别标记。

源方法选择仅在预先指定的一个seed完成，后续seed复用方法配置但重算自己的源资源。
完成后停在先导评审，不查看control_test或2021/In-the-Wild新效果，不自动开展最终评分。
