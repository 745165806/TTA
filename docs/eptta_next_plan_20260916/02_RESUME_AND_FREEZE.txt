执行00_PLAN.md的工单2。
mm批准恢复计划SHA-256：<填写本项目真实计划的完整hash>。
授权范围、GPU、存储、路线和预算均以该计划为准；不擅自扩展或重走已通过审批。

1. 再核对现场状态与计划身份。逐模型按已批准的原身份恢复、新身份续训或从头新run执行，
   任何前置条件变化就停止受影响路线，不自动忽略hash、不偷换入口依赖。
2. 先在独立验证目录完成真实生产loader上的“连续运行”与“保存—退出—新进程恢复”对照。
   核验数据顺序/增强、loss、参数、optimizer、LR/scheduler、buffer和随机状态。
   仅在支持和已验证的边界声明恢复；短程近似一致不扩大成任何环境bitwise相同。
3. 续训用last，不从best回滚。若实际保存边界确认报告进度，AASIST继续14–99、SSL继续91–99。
   终点是原100epoch预算，不另加100轮、不重置学习率；新run按批准预算执行。
4. source_val独占checkpoint选择。沿用原并列规则，尤其SSL的多个0%EER，不能用目标结果挑epoch。
   旧best/last按旧合同及选模记录核验；不能追溯造epoch权重或偷改原worker身份。
5. 实际完成后才FINALIZED，然后运行R4：embedding/head/logits/score、类别映射和score方向，
   首次/重复前向、mode/buffer、设备/dtype、batch/分片，以及原模型/导出模型parity。
   容差按预先设定模式；底座冻结和adapter仍可求梯度分别验证。
6. 缺入口或集成检查时补最小实现和测试。训练代码/导出代码身份分别保留，失败不绕过R4门禁。
7. 更新状态摘要、真实恢复/训练/选模日志、FrozenModelBundle、R4报告与成本。
   准备数值完整的R5/R6计划和真实命令，停止等待源先导运行授权。

可继续已授权的外部元数据/adapter工作，不产生新目标效果。
一个底座合格即可准备其源先导，不因另一个受阻而一并停工，也不据目标成绩取消另一个。
