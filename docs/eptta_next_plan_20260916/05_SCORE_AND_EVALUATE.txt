执行00_PLAN.md的工单5。
mm批准确认性plan SHA-256：<实际完整hash>。
逐字节核验计划；目标数据、模型、方法或身份不匹配时拒绝受影响任务。

1. 按完整模型×seed×方法×目标矩阵执行无标签评分。
   评分worker只读取必要白名单，不可访问class/attack/source sidecar，不按目标名称选参数。
2. EP每样本固定初始化并reset R、optimizer和梯度。底座/固定U/M/tau0不在目标数据拟合。
   原视图是当前实际输入；batch并行不允许适配状态/统计跨样本共享。
3. 适配失败只能按封存规则退回合法Frozen分数；Frozen无法评分时阻塞，不补零、不删样本。
   分别记录成功适配、安全不更新、数值回退，不用coverage=1冒充成功适配100%。
4. 所有预定分数通过准确覆盖和score seal后，独立evaluator统一关联标签。
   矩阵缺项时保持阻塞，或在未看效果前按审批修订范围，不静默删方法/目标/seed。
5. 按目标分别输出EER/AUROC、各seed和均值±标准差、配对ΔEER、
   固定源tau0两类错误率、有益/有害翻转、预定配对分组区间、回退/覆盖及完整成本。
   不合适的单类分组指标标N/A；适用的LA可报min t-DCF，DF/ITW不编造ASV成本。
6. 不把目标EER对应阈值回写成tau0。不同尺度基线仅按预定源校准口径另表报告。
   2000次bootstrap等统计规则遵守已封存分组/seed设定，不按显著性改方法。
7. 2019已暴露参照、2021相关来源、经核实未参与开发的外部目标分别说明证据范围。
   保留负结果、失败、缺项；H1–H7依据原定义分别标支持/部分支持/不支持/未验证。
8. 缺报告代码则补最小实现和反例测试；发生工程错误要记录修改和受影响工件重算。
   一旦已经查看目标指标，任何方法更改必须标post-hoc，不能重新包装同数据为原确认实验。

交付可复算评分/评价与计划身份、完整结果表、失败/成本/不确定性报告和结论边界。
不回头挑checkpoint、recipe、seed、阈值、方法或目标范围。
