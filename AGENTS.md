# EP-TTA v0.1.0 开发规则

- 唯一主合同是 `docs/DESIGN.md`。项目及自有 schema 初始版本均为 `0.1.0`。
- 本机只开发和运行合成测试；真实数据、预处理、训练、缓存和实验在远程 Ubuntu 执行。
- 不自动下载音频或模型、不自动 SSH、不安装 CUDA、不解引用占位资源、不覆盖用户文件。
- AASIST / SSL-AASIST 鉴伪参数必须由本项目训练；通用 SSL 前端初始化单独记录，不得替代任务训练。
- 未审核的 raw/label/group/preprocess/split/recipe 保留 null；正式执行必须拒绝，不猜列、不填默认标签。
- 源训练只接受 fit/source_val。目标标签、攻击、路径语义、历史及整体统计不得进入适应接口。
- 增量数据不改旧 snapshot；EP 每条重置，独立 R 不跨样本/跨卡归约。源训练的 DDP 是独立合同。
- PASS 必须引用真实命令、退出码和日志；缺依赖标 NOT_RUN。工程、远程运行与科学结论分别记录。
- 本轮仅 L0–L2；后续真实执行入口必须显式失败，不能返回伪成功或 toy 回退。
